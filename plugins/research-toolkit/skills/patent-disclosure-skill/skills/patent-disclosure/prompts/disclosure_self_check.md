# 交底书生成后自检（Step 8，内部执行）

自检结果用于**修订正文**，默认**不单独输出自检报告**；用户索要时可单独提供。**不得**将自检清单作为交底书一章写入正文。

**按类型选用**：

| 类型 | 必做 |
|------|------|
| 发明 | §8.1–§8.3（全文） + **§8.7**；含公式时 §8.2 必核 |
| 实用新型 | §8.3 通用项 + **§8.4** + **§8.7**（客体组按结构类口径；§8.1–8.2 中明显属「算法/系统/公式」的条目可跳过） |
| 外观设计 | §8.3 中与命名/查新/文末清洁相关项 + **§8.5**（**不做** §8.7） |

细则成文入口：`invention|utility_model|design/disclosure_builder.md`。

## 8.1 逻辑与闭环（发明为主）

- [ ] 技术方案是否形成完整闭环（发现→处理→扩展/输出）？
- [ ] 模块之间关联关系是否清晰？上下游是否衔接？
- [ ] 分支样本（如边界样本、低置信度样本等）是否有明确处理路径？
- [ ] 依赖项（如类别中心、动态阈值等）是否有来源说明？
- [ ] **第五章「技术关键点」与第三章「系统流程」是否呼应？** 第五章以论点概述为主，技术细节在第三章；每条保护点须为可实施机制，**不是**仅罗列标题对象或「有三个××」。
- [ ] **3.1 对象与术语**：是否已按场景与参与者、标题对象如何进入系统、高频**领域词**定义三小段铺垫？
- [ ] **标题贯穿**：案件名称中的领域实词是否在 **3.2 框图、3.4 流程、第六章**作为同一执行主体出现，且未被更空的上位词替换？
- [ ] **术语贴合话题**：3.3 / 3.5 / 第五章用词，只看标题和 3.1 场景是否像本领域？若像另一技术领域（账本、指纹、水位、幂等、锚点等只是错位例，随案而变），是否已换成领域表述而非靠定义保留？机制是否仍说得清？
- [ ] **3.3 与 3.4 对应**：3.3 是否按框图拆成独立模块？3.4 正文是否逐项对应图中 S1—Sn（无漏步、无多步混成一段且不点序号）？
- [ ] **实施例**：是否有 2～3 个与 3.1/3.2 同名的实例（职责、能力、交付物、依赖），并覆盖主路径 + 一条第三章已有分支（而非 3.4 缩写）？

## 8.2 公式与参数一致性（发明；含公式时必做）

**Step 8 必做（含公式时）**：除符号体例与跨节一致外，须**主动复核公式是否正确、公式逻辑是否与第三章叙述一致**（等同用户会提出的「检查公式和公式逻辑是否有误，有误请调整」）；发现问题**直接改稿**，勿仅提示用户自行修改。

### 符号与体例

- [ ] **formula_plan（发明含公式）**：案件目录已有 `formula_plan.yaml`；每条式已标 `origin`；`source` 未改写成范式模板且有 `source_ref`（或等价出处）；`agent` 的 `paradigm_id`∈合并后的范式库；材料未采用的式已进 `omitted`；`check_formula_plan.py` 通过或等价手检通过
- [ ] **保真（有材料公式时）**：3.4.1 主式能否对上材料（项、算符、条件未被丢掉或换成库内简单式）？
- [ ] **符号表（3.4.1）**：全文含公式时是否已设 **3.4.1** 并 **先定义符号**（含义、下标、量纲）；式 (1) 及后文每个符号是否均已在表中定义？
- [ ] **维度下标**：是否存在 `^{cpu}`、`^{mem}`、`^{io}` 等 **上标表示维度** 的写法？若有须改为 `b_{i,\mathrm{cpu}}`、`a_{j,\mathrm{mem}}` 等 **下标 + `\mathrm{}`** 形式（见 **`disclosure_builder.md` §7.7**）
- [ ] **装饰音**：是否出现 `\tilde`/`\hat`/`\bar` 等？若有改为独立符号（如平滑量 \(A\)）或范式 `ema_smooth`
- [ ] **字母多义**：同一字母是否兼指任务侧与节点侧等不同对象？若有须拆分符号（如任务用 \(b\)、节点用 \(g\)）
- [ ] **公式表述一致**：同一物理量在不同公式、段落中是否 **同形同义**（如权重公式、调整系数、匹配分 \(s_{ij}\) 等）
- [ ] **LaTeX 体例**：行内/块级分隔符是否全文统一（`\(...\)`/`\[...\]` 或 `$`/`$$` 二选一）；块级公式是否尽量单行；是否避免 `\text{且}` 等易渲染失败写法
- [ ] **行内分隔符（硬项）**：定稿 md **不得**把含 `\mathrm` / `\,` / `_{` 的式子写成普通括号（如 `(M_{\mathrm{total}})`、`(m_{\mathrm{silica}}=60\,\mathrm{g})`）。须为 `\(…\)` 或 `$…$`。预览里 `\(` 看起来像 `(`，勿删反斜杠。跑 `python tools/latex_delimiters.py -i <定稿.md>`，有 hits **不得出 Word**；改正后对**该时间戳 md**重跑 `mermaid_render.py` / `md_to_docx.py`，禁止用未改正的 `draft.md` 出交付 `.docx`
- [ ] **3.5 与符号表**：3.5 参数表「符号」列是否与 3.4.1 **逐字同形**
- [ ] **阈值范围**：与正文一致（如 0.5–1.5、0.8–1.2 等）
- [ ] **参数命名**：全文统一，避免同义不同名混用（公式符号与 **§8.1 场景术语**分开查：场景词换名须整族替换，见 merger / correction）
- [ ] **实施例数值**：与 3.5 关键技术参数对应，不冲突；且与 `formula_plan.numeric_example` 可互相印证

### 公式正确性与逻辑（必核）

- [ ] **式面正确性**：各式是否无明显笔误（运算符、括号配对、上下标错位、同一式内符号与符号表不符、缺项/多写项、指数或分母写错等）？
- [ ] **可算性**：先跑 `python tools/check_formula_plan.py -i …/formula_plan.yaml --eval`；agent 式代算失败须改式或改数值例。source 复杂式脚本跳过则手算或从缺，**禁止**改成库内简单式以通过代算
- [ ] **量纲常理**：相加各项是否已无量纲/归一？防零除 \(\varepsilon\) 是否与分母同量纲？有无裸 `max(1, 容量)`？
- [ ] **约束与不等式方向**：公式中的 `\leq`/`\geq`/正负号是否与文字含义一致（如「饱和度」「有余量」「限频间隔」等）；多条件「且/或」是否与 3.4 流程分支一致？
- [ ] **公式与流程互推**：3.4.1 主公式（含式 (1)）及衍生式，能否与 **3.4 流程说明**（各步骤 S1—Sn）、**3.3 模块职责** 逐步对应？叙述中的打分、重排、限频、窗口聚合等是否在公式中有体现，**无**「文字一套、公式另一套」？
- [ ] **边界与特殊情形**：空队列、单节点、分母为零、指标缺失、达到上下界等情形，公式或正文是否给出合理处理或说明（至少不与主公式矛盾）？
- [ ] **量纲与取值**：符号表声明的量纲/取值范围是否与公式用法一致（如权重为正、比例在 0—1、计数为非负整数等）？
- [ ] **修订联动**：若修正公式或逻辑，是否已同步改 **formula_plan**、**3.4 文字**、**3.5 参数**、**第六章实施例** 及 **3.4.1 符号表**（见 §7.7 跨节一致）？改正 LaTeX 分隔符后，是否已用改正后的时间戳 md **重出** `.docx`（未沿用旧 Word）？

## 8.3 格式与引用

- [ ] **迭代路径**：若本次走 `merger.md` / `correction_handler.md`，对话中是否已含 **`## 合并摘要（留档）`** 或 **`## 纠正摘要（留档）`**（按该 prompt 字数要求）
- [ ] **修订对话记录**：案件目录是否已追加 **`交底书修订对话记录.md`** 一条（含记录时间、用户说明摘要、本轮交付文件名、摘要摘录），见 **`iteration_context.md`**
- [ ] **交付文件名**：凡落盘交付的交底书是否均为 **`{案件名}_{YYYYMMDDHHmmss}.md`** 及同名 `.docx`（§7.3 第 5 点，**含首次定稿与迭代**），未无故覆盖旧稿
- [ ] **图示（按类型）**：发明须 fenced mermaid → `tools/mermaid_render.py` 转 PNG，无 ASCII 框图；流程图节点若用 S1/S2 作 id，**可见标签须含该序号**（`S1["S1 …"]`，禁止 `S1[只写中文]`）；实用新型以 **`figure_plan.yaml`** 入文线稿为主（CAD/实拍不入文）；外观 **实拍 + 线稿** 均入 md 与 Word。正文「如图/见图 N」与清单 `fig` 一致。`.md` 已交付；发明须 `.docx`；实用建议 docx；外观须同名 `.docx`
- [ ] **点名图**：用户要求入交底的图已拷入 `assets/` 并编号嵌入，或已说明不入文原因；未扫 `node_modules` / 前端素材目录；未把技术环境图当成外观 `photo_scene` 丢掉
- [ ] **figure_plan（实用/外观）**：案件目录存在 `figure_plan.yaml`（或 json）；主题/材料若本轮有变，清单已重评（含 `relates_to`）；未入文图有 `reason`；总装+局部或入文多视时图际关联已填写
- [ ] 章节引用：如「详见 3.4.1」须指向真实存在的章节
- [ ] **1.1 / 在先对比**：列举的专利/文献/在先外观是否均附有**可访问且与著录项一致**的公开 URL（非编造）；是否与 `prior_art_search.md` 及检索说明自洽；**检索说明**是否**未**出现 `cnipa_epub_search.py`、WebSearch/降级等内部流程用语；发明/实用是否经 IPC 第二轮（或已说明无分类号）；外观是否经 **LOC** 第二轮；1.1 是否**优先第二轮**并按分类/手段过滤；第二轮不足 4 条是否已从第一轮**同分类号**回补（仍少可仅分类号/相邻号再查），**未**编造条目凑数、**未**把第一轮合并全集写入 1.1
- [ ] **查新摘要（国知局）**：凡 **`EPUB_HITS_JSON`** 中带非空 **`abstract`** 的条目，正文概括**是否体现对摘要的理解**；无 `abstract` 时是否已按 `prior_art_search.md` 补全理解
- [ ] **查新类型**：`--type` 与文头专利类型一致（invention / utility_model / design）
- [ ] **脚本判读**：未因 PowerShell stderr / 乱码 / `NativeCommandError` 把退出码 0 的查新或定稿当成失败；`LATEX_DELIM: hits=` 大于 0 视为分隔符未改好、Word 未出，须改正后重跑，不得交付坏公式 Word
- [ ] **文末清洁**：正文**无**技能仓库名、`examples/`、`disclosure_draft` 路径、「教学示例」「虚构」「不构成法律承诺」等**元信息脚注**（若存在则删除）
- [ ] **权利要求偏向点（对话）**：定稿对话是否按 **`prompts/delivery_confirm.md`** 输出 **`## 交付后请确认`**（发明侧重见该文件第 2 条）；侧重点源于本稿，**无**凭空捏造
- [ ] **公式 PNG（对话，发明）**：stderr 若有 `omml_text_fallback` 或 `OMML_FAIL:`，是否已在 **`## 交付后请确认` 第 1 条**以 **`(a)` 标签**反问是否装 matplotlib 并以 `--math-render` 重出 Word；未得「是」则未安装。若同轮 STEP 也待问，是否按 `delivery_confirm.md`「同一轮至多一个是/否」只问 `(a)`、STEP 改为陈述
- [ ] **政策感知低频提示（对话，可选）**：若适用，是否已写入 **`## 交付后请确认` 第 4 条**（**`skills/patent-exam-policy/prompts/soft_nudge.md`**；**至多一句**；未写入正文；未因此自动进入政策简报）
- [ ] **STEP 解析（对话）**：cad_scan 为 `ask_enable_step_parse` 时，是否已在 **`## 交付后请确认` 第 1 条**以 **`(b)` 标签**反问；未在 Step 2 中断成文；未得「是」则未安装依赖。仅原生 CAD 则第 1 条已提示导出 STEP（不算是/否）。若同轮公式 PNG 也待问，是否让位于 `(a)`

## 8.4 实用新型专项

- [ ] 文头 **专利类型：实用新型**
- [ ] 第三章可追溯 StructureSchema（部件/连接/布局）；`uncertain` 未写成既定公差
- [ ] 第五章为装置/结构书式，非纯方法独立点
- [ ] 未把外观美感或算法步骤当作构造创新主线
- [ ] 附图「如图 N」与 `figure_plan` 的 `fig`/`path` 一致（非散落 assets 临场挑选）
- [ ] 总装+局部入文时 `relates_to` 已写；正文「局部/剖视」表述与之对齐；跨图件号一致
- [ ] 入文图仅为合格 lineart；CAD/实拍未误入正文  
- [ ] 结构线稿已走 image_gen：存在 `structure_lineart_brief`（或已有合格线稿故跳过生成）；已按件拼装（`structure_lineart_compose`：磁盘上有 `parts/{视}_{id}.svg`，总图相对引用，一层一件号，未拆筋/齿/螺栓，未整图 clip）；件号与 `structure_schema.parts` 一致；已叠标后读图核对引出线落点（非仅 overlay 脚本合法）；未误用 `design_lineart_*`

## 8.5 外观设计专项

- [ ] 文头 **专利类型：外观设计**
- [ ] 设计要点可追溯 AppearanceSchema；只写可见造型/图案/色彩
- [ ] 已判 `product_form` 并按 `claimed_faces` 定正投影（非默认六视）；名称用官方全称（主视图、后视图、左视图、右视图、俯视图、仰视图、立体图）；`omitted_views` 已写或为 `[]`；要点落面缺源图才用 `uncertain`；入文视图与 `figure_plan` 一致
- [ ] 入文多视/局部的 `relates_to` 已写（或可说明无需关联）；跨图造型无矛盾
- [ ] 外观线稿已走 image_gen：存在 `design_lineart_brief`（或已有合格线稿且过 `design_view_cnipa.md` 故跳过生成）；未过 4.2 的已有线稿已重画，未走 `existing_lineart`；干净实拍与线稿均已入 md 与 Word；CAD 未入文；透视棚拍未当合格 `photo_clean`；实拍未标成 `kind: lineart`
- [ ] 线稿为正投影（立体图角色正确）、各视比例一致、无尺寸线/中心线/阴影线；图题在图外正下方，图内无图号；画布对准 JPEG、不超过 150mm×220mm、72–300 DPI
- [ ] `not_design_signals` 已处理（反问改类型或已从要点删除）
- [ ] 未写内部电路、受力、工艺作为设计要点

## 处理原则

发现问题则**直接修订正文**后再交付。  
**公式类（发明）**：Step 8 须完成 §8.2 核对；有误则改公式并联动修订相关章节，**不要**只向用户报告问题而不改稿（除非缺少必要技术事实、无法从正文与材料推断正确写法）。  
**术语 / 标题贯穿 / 实施例（发明）**：§8.1 相关项不通过则改 3.1、框图、流程、实施例或第五章后再交付；换叫法须整族替换，机制不丢。  
**法定要件（发明 / 实用新型）**：**§8.7** 的「三段绑定」与「删除测试」是硬项——技术问题含手段词、第五章未分必要/优选、Fk 口径各章不一致或正文靠 F 号跳转才能读懂，均须**改稿后**再交付。三态都允许成文。**不过**时禁止假 D1、禁止空喊「创新性强」；**带疑点过**须保留证据边界。按 `prior_art_search.md`「门禁」三态处理。

## 8.6 专利布局 / 专利围栏旁路（交付后，不入正文）

首篇定稿后 **`Read` `prompts/fence/guardrails.md`**，把「要不要做保护型 1+N」写进 **`## 交付后请确认` 第 3 条**（`prompts/delivery_confirm.md`），不要先打分再问。用户点名「专利布局 / 专利围栏 / 族树 / 做围栏」可强开，但仍须分解 → 突围 → 矩阵 → 立项说明。  
**禁止**未确认立项说明就批量写多篇。迭代的非首次交付默认省略第 3 条。  
**禁止**在该节把申请文件、案卷或专利地图列为下一步。

## 8.7 法定要件与创造性主线（发明 / 实用新型，必做）

本节针对「能不能授权、保护范围合不合理」，**不是**文风检查。成文纪律见 **`invention/disclosure_builder.md` §7.10**。**外观设计跳过本节**（走 §8.5）。

### 创造性主线（D1 与 Fk）

- [ ] **D1 出发点**：过 / 带疑点过时，1.1 是否点名最接近的一篇（公开号 + 标题 + **一句选定理由**），且**未**把「命中列表第一条」或「公开日最新」当成最接近、**未**对五篇做马赛克式并列逐特征？**不过**时是否**未**点名假 D1、未编造公开号，并写明未钉住对比文件？
- [ ] **逐特征比对表**：过 / 带疑点过时，1.1 是否有以 D1 为主的技术维度表，**编号单独成列**，「本案怎么做」写满构造，表后有特征一览？是否**未**用「是（F1）」当最后一列、**未**把列名叫「本专利权要」？D2 是否只以补行出现？**不过**时 Fk 是否相对材料/常规做法在一览中编号，并标明未经对比文件钉死？维度是否为**技术维度**（**非**申请人 / 年份 / 是否授权 / 文献篇数）？
- [ ] **Fk 口径与可读性**：1.1 一览、5.1 是否同一编号、同一构造？1.2 / 二 / 四是否为完整中文句（F 号最多句末括注），且**未**用「对应 F1：」「由 F1 带来：」当标题或句首？三 / 六是否**未**把 Fk 当主语（发明用模块/步骤号，实用用件号）？离开 F 号是否仍能读懂？
- [ ] **1.2 有指向**：过 / 带疑点过时，每条缺点是否指向 D1 或 D2 一侧的**具体**不足？**不过**时是否指向材料所述现有做法或本领域常规的具体不足？是否**未**写「现有技术普遍存在不足」或无指向的「创新性强」？
- [ ] **三段绑定（硬项）**：每条 Fk 能否填出「特征 → 技术效果 → 实际解决的技术问题」？**第二章技术问题句里是否出现本发明的手段词**（如「画像」「限频」「重排」这类本案手段，随案而变）？出现即不合格，须改写成效果导向表述（正/反例见 §7.10）。第二章是否只写问题句、未在句后点名 Fk？
- [ ] **协同整体论述**：功能上相互支持的特征是否**整体**陈述效果，而非拆成互不相干的独立条目（拆开会低估创造性）？
- [ ] **第四章挂钩**：每条优点是否把带来该效果的构造/机制写进句子？有无「提升效率」「增强鲁棒性」这类无主优点？是否**未**用「由 F1 带来：」当句首？
- [ ] **删除测试**：第五章是否分 **5.1 必要技术特征 / 5.2 优选特征**？逐条做过删除测试（删掉后仍能解决第二章问题的，是否已移入 5.2）？5.1 每条是否带「对应 3.x / Sx / Mx / 式 (n)」？
- [ ] **不越界**：5.1 是否**未**写成「根据权利要求 1…」正式权项句式（交底书不是权利要求书）？
- [ ] **单一性**：若存在 ≥2 组无共同必要特征的发明点，是否已写明**总的发明构思**或建议拆案（仍允许本篇成文，不因此停笔）？
- [ ] **门禁留痕**：案件目录是否有 **`查新与区别定位_*.md`**，且文首有门禁三态与证据级别？**不过**是否降级成文且无假 D1？**带疑点过**时 1.1 是否写了证据边界、未装成已读全文？

### 客体（软件 / 算法 / 商业方法类必核）

- [ ] **技术三要素**：是否同时具备 **技术手段**（对系统、设备或流程的改造，非纯数学、非纯管理规则）、**技术问题**（如调度震荡、资源错配，非商业或经营目的）、**技术效果**（可观测的技术指标改善）？
- [ ] **失败信号**：全篇能否被改写成「一套纯计算方法 / 打分公式」而不依赖任何技术环境？若能，是否已在 3.1 与第三章**绑定到具体技术环境**（执行主体、被控对象、数据来源与作用对象）？
- [ ] 是否**未**把纯业务规则、经营策略或单纯的信息呈现当作保护点主线？

### 参数可获得性（充分公开）

- [ ] **来源**：每个关键阈值 / 权重是否交代来源，三选一 —— **经验值** / **由实验或统计得到**（如历史数据分位）/ **可在给定范围内调**？**禁止**魔法数直接出现在正文而无出处。
- [ ] **单调性**：是否说明调大 / 调小各偏向什么（如「阈值增大则重排更保守：震荡更少、响应更慢」）？
- [ ] 是否**未**出现只有功能限定、却无任何获得方式的参数？

### 表述风险（防自伤）

- [ ] **绝对化用语**：是否慎用「必须、唯一、最佳、最优、所有情况」等（易被读作对保护范围的限定）？宜改「优选」「在一些实施例中」。
- [ ] **背景技术不自认**：第一章是否**未**写入仅本发明才有的技术内容（写进背景技术等于自认现有技术）？
- [ ] **客观**：第一章是否**未**贬低他人方案，只客观陈述局限？
