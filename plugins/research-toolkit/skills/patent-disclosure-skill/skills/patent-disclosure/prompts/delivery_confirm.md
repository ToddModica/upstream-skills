# 交付后请确认（对话末块 · 不入交底正文）

定稿路径、案件边界、查新对比、第五章主线写在**本块之前**。  
本块是回复**最后一个二级标题**，标题必须是：

## 交付后请确认

只放「请用户决策」。不得写入 `.md` / `.docx`。  
**禁止**在此节把「申请文件 / 案卷 / 专利地图」列为下一步。

带前缀编号。无内容则**整条省略**（不要写「无」凑数）。口令用引号，与根 `SKILL.md` 触发词一致。

### 1. 内容待确认 / 可调整

**先写陈述性内容**：材料没钉死、正文故意未写死的参数/分支/后端是否对称等。这部分**不要求**用户回是/否。

**依赖开关反问**（如有）另起一行放在本条末尾，**每个带字母标签**、各一句、明确请回 **是** / **否**：

- **(a) 公式 PNG**：stderr 出现 `OMML_FAIL:` / `omml_text_fallback=` / `MATH: … text=`（>0）时，问是否装 matplotlib（约 100MB，含 numpy；图片在 Word 中**不可再编辑**）。
- **(b) STEP 解析**：`cad_scan` 为 `ask_enable_step_parse` 时，问是否开启多视角解析。
- 仅原生 CAD、无 STEP 文件时：只**提示**导出 STEP，**不算**反问、不带是/否。

**同一轮至多一个是/否（硬项）**：(a) 与 (b) 同时满足时，**只问对本轮交付影响更大的那个**——公式已 fallback、直接影响当前 `.docx` 可读性时优先 **(a)**，否则 **(b)**；另一个改写成一句陈述（「如需…可后续告知」），**不带**是/否。避免用户回一个「是」指向不明。

未得明确「是」（含未回复、回复含糊）→ **不装任何依赖**，保持当前交付物，**不要**再问第二轮。

### 2. 本稿可调整

对应原「权利要求偏向 / 第五章侧重」。给出**两组本稿已有**的对举（发明可偏书式：方法/系统；实用/外观偏已有结构或视图要点）。**禁止捏造**本案未写的场景或模块。用户说侧重后走 `iteration_context.md` → `merger.md`，**另存**新时间戳稿。

句式可参考：更偏「……」或更偏「……」（均为本稿已有路线，仅为强调重心）；说侧重即可另存一版。

迭代再交付仍须有本条（可缩短），禁止捏造。

### 3. 下一步 / 旁路（专利围栏）

**仅首篇** Step 8 定稿后出现；迭代非首次默认省略。  
`Read` `prompts/fence/guardrails.md` 后写入本条，**只问一句**：若要在首篇之外做保护型 1+N（分解 → 突围 → 功效矩阵 → 立项说明，确认后再分件），直接说「做围栏」（「专利布局 / 专利围栏 / 族树」同义）。不要先打分再问，不要写成高价值达标。未答应则结束旁路。

### 4. 旁路调研补充

按 `skills/patent-exam-policy/prompts/soft_nudge.md`：满足条件才出，整条至多一句。口令「政策简报」。不自动进简报、不改技能。抑制条件命中则**整条省略**。

带反问时，第 1 条末尾形如（本轮只出现一个是/否）：

```
   **(a) 公式 PNG：** 式 (2) 未能转成可编辑 Word 公式，已保留 LaTeX 原文。是否安装 matplotlib（约 100MB）改用图片？请回「是」/「否」。（STEP 多视角解析如需开启，可后续告知。）
```

### 样例（实用新型首篇；无 STEP/公式时）

```
## 交付后请确认

1. **内容待确认 / 可调整：** ……（喷孔、回路、密封圈、介质与温升、后端是否对称等；有材料后再写，当前未写死。）
2. **本稿可调整：** 第五章可更偏「……」或更偏「……」（本稿已有）。说侧重即可另存一版。
3. **下一步 / 旁路：** 若要在首篇之外做保护型 1+N，直接说「做围栏」。
4. **旁路调研补充：** 近期专利审查口径可能有调整；若想对照国知局官网看这份交底有没有过时写法，可说「政策简报」（只出参考，默认不改技能）。
```
